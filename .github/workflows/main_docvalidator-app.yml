name: Deploy Next.js to Azure Web App - docvalidator-app

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (Next.js standalone)
        run: npm run build
        # Requires next.config.js with: output: 'standalone'

      - name: Prepare deploy package (use standalone package.json)
        run: |
          rm -rf package
          mkdir -p package
          # Copy the standalone server, INCLUDING its own package.json (with "start": "node server.js")
          cp -r .next/standalone/* package/
          # Static assets
          mkdir -p package/.next/static
          cp -r .next/static package/.next/static
          # Public assets (if any)
          if [ -d public ]; then cp -r public package/public; fi
          # IMPORTANT: do NOT overwrite package/package.json with the root one

      - name: Create zip artifact
        run: (cd package && zip -r ../app.zip .)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-zip
          path: app.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: app-zip

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_B49321364C594CF3BFA5775AAB543410 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_E878D9CF0F644786B7E0358E2DBA2EC9 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_E40F12ECD2244322B9CA34F8F2215DCF }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: docvalidator-app
          slot-name: Production
          package: app.zip
